// import { stopSubmit } from "redux-form";
// conctans && API

let initialState = {};

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
